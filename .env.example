# Database
DATABASE_URL=postgres://axon:axon@localhost:5432/axon?sslmode=disable

# Chief endpoint (for event forwarding)
CHIEF_ENDPOINT=http://localhost:9100

# Internal API secret (shared with Chief)
INTERNAL_SECRET=your-internal-secret

# Server port
PORT=8080
