# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=axon
DB_USER=axon
DB_PASSWORD=
DB_SSL_MODE=disable

# Chief URL (for event forwarding)
CHIEF_URL=http://localhost:9100

# Internal API secret (shared with Chief)
INTERNAL_SECRET=your-internal-secret

# Server port
PORT=8080
